{
    "name": "Python 3",
    "image": "://microsoft.com",
    "features": {
        "ghcr.io/devcontainers/features/common-utils:2": {}
    },
    "updateContentCommand": "sudo apt-get update && sudo apt-get install -y espeak ffmpeg libespeak1 && pip3 install -r requirements.txt"
}
